# 検証記録

初回実装の検証を実施中です。結果が確定したらこのファイルを更新します。

- Nix 2.35.2 / ARM64 Linux コンテナで `nix flake check --no-build --all-systems`: 成功。
- x86_64 / ARM64 の共通システムと統合 VM 定義: 評価成功。
- ARM64 共通 OS + Home Manager: 実ビルドを実行中。
- 実機情報・ユーザーの SSH 公開鍵: 未提供。本番ホストは無効状態。
- 実サーバーへのインストール、起動、接続、データ移行: 実施していません。

この時点の評価成功は、実機への適用成功や統合 VM の実行成功を意味しません。
